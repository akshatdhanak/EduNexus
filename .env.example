# ───────────────────────────────────────────────────────────
# EduNexus — Environment Variables
# Copy this file to .env and fill in your values
# ───────────────────────────────────────────────────────────

# Django Core
SECRET_KEY=change-me-to-a-long-random-string
DEBUG=0
ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com

# Database (PostgreSQL in production)
DATABASE_URL=postgres://edunexus:password@localhost:5432/edunexus

# For docker-compose local dev
DB_PASSWORD=edunexus_secret

# Google Gemini AI (NL-to-SQL chat)
GEMINI_API_KEY=your-gemini-api-key-here

# CSRF trusted origins (comma-separated, include https:// prefix)
CSRF_TRUSTED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Static/Media (optional — for S3/cloud storage later)
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_STORAGE_BUCKET_NAME=
# AWS_S3_REGION_NAME=

# Email (optional — for password reset etc.)
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_HOST_USER=
# EMAIL_HOST_PASSWORD=
# EMAIL_USE_TLS=1
